
#include <stdio.h>

int main()
{
    int n,i,max=0;
    scanf("%d",&n);
    while(n!=0)
    {
        i=n%10;
        if(i>max)
        {
            max=i;
        }
        n=n/10;
    }
    printf("%d",max);
    return 0;
}
