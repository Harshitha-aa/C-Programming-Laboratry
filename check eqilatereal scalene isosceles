#include<stdio.h>
int main()
{
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    if(a==b==c)
    {
        printf("equiateral");
    }
    else if((a!=b)&&(b!=c)&&(c!=a))
    {
        printf("scalene");
    }
    else if(a==b||b==c||c==a)
    {
        printf("isosceles");
    }
    
}
